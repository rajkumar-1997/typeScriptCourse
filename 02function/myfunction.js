function addTwo(num) {
    return num + 2;
}
addTwo(4);
function toUpper(str) {
    return str.toUpperCase();
}
//? is for optional parameter
var isLogin = function (name, email, isPaid) {
};
isLogin('dg', 'fd');
var isSignup = function (name, email, isPaid) {
    if (isPaid === void 0) { isPaid = false; }
};
isSignup('sff', 'fsf');
toUpper('raj');
